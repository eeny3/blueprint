import 'package:one_trade/domains/registration_step_domain/registration_step_domain.dart';

RegistrationStepDomain mapToRegistrationStepDomain(
    String id,
    ) {
  return RegistrationStepDomain(
    id: id,
  );
}

